# ─── Salin file ini ke .env lalu sesuaikan ───────────────────────────────────
# cp .env.example .env

# Database
DB_ROOT_PASSWORD=rootsecret_GANTI_INI
DB_NAME=antigravity_db
DB_USER=antigravity
DB_PASSWORD=antigravity123_GANTI_INI

# Admin dashboard secret (header X-Admin-Secret)
ADMIN_SECRET=pplglaser

# Port nginx yang diekspos ke luar (default 80)
HTTP_PORT=80

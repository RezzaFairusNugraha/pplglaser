# ─── Salin file ini ke .env lalu sesuaikan ───────────────────────────────────
# cp .env.example .env

# URL backend FastAPI (sudah jalan di server/cloud terpisah)
BACKEND_URL=https://backlaser.pplgsmkn4.my.id

# Admin dashboard secret (header X-Admin-Secret) — harus sama dengan backend
ADMIN_SECRET=pplglaser

# Port yang diekspos ke luar untuk frontend
HTTP_PORT=3000
